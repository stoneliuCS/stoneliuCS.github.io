Marker for the Typst project root (matches build.py's `--root typst`).
tinymist (Neovim LSP) resolves the project root to this directory via
`root_markers` in ~/.config/nvim/lua/config/lsp/tinymist.lua, so absolute
(`/lib/...`) and relative (`../../lib/...`) imports resolve the same as the build.
